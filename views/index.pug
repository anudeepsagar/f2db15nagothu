extends layout

block content
  h1= title
  p Welcome to Assignment10
  body 
  button(type="button",class="btn btn-secondary", onclick ="doBump()") press Me
  div#bump Bump the count
  a(href="elephant") elephant Results
  script.  
    var count = 0
    var bump = 1
    function doBump() {
      count = count + bump 
      bump = bump + 1
      document.getElementById("bump").innerHTML = "Count "+ count +" Bump "+bump;
    }
    
  